<script setup lang="ts">
import QuestionBank from '../../.vitepress/theme/QuestionBank.vue'
import questionBank from '../../data/question-banks/hardware.json'

function questionsFor(keys: string[]) {
  const selected = new Set(keys)
  return questionBank.questions
    .filter((question) => selected.has(`${question.exam}-${question.question_no}`))
    .sort((a, b) =>
      b.exam.localeCompare(a.exam) ||
      b.question_no - a.question_no ||
      a.part_no - b.part_no
    )
}

const embeddedQuestions = questionsFor([
  '202605-33',
  '202505-11',
  '202405-12',
  '202311-38',
  '202211-10',
  '202111-3',
  '202111-7',
  '201811-7',
  '201611-3',
  '201411-6'
])

const chipQuestions = questionsFor(['202211-11'])
</script>

# 嵌入式系统

本页从计算机组成原理中拆出嵌入式系统相关知识，结合分类真题整理。题库中明确归为“嵌入式”的真题<strong>10 题</strong>；AI 芯片另有<strong>1 题</strong>，作为相关的专用处理器专题一并学习。

| 复习分类 | 真题数量 | 主要内容 |
| --- | ---: | --- |
| 嵌入式 | 10 题 | 系统组成、处理器、实时性、软件架构、MMU 与操作系统 |
| AI 芯片 | 1 题 | GPU、FPGA、ASIC 与 AI 芯片技术架构 |

## 1. 嵌入式系统：从应用约束反推设计

### 1.1 定义与组成

嵌入式系统是<strong>为了特定应用而专门构建，并将信息处理过程与物理过程紧密结合</strong>的专用计算机系统。它通常从传感器或人机接口采集输入，经过计算处理，再通过接口控制受控对象。

教材归纳的嵌入式系统组成包括：

| 部分 | 作用 |
| --- | --- |
| 嵌入式处理器 | 系统的运算和控制核心 |
| 支撑硬件 | 存储器、定时器、总线、I/O 接口和专用硬件 |
| 嵌入式操作系统 | 管理处理器、存储器、设备、任务和通信等资源 |
| 支撑软件 | 提供公共服务、开发和调试能力 |
| 应用软件 | 完成具体设备或行业目标 |

### 1.2 高频特点

嵌入式题目不要只记“体积小”。更稳定的判断方法是看它是否围绕<strong>特定应用和资源约束</strong>展开：

- 专用性强，软硬件按目标应用进行裁剪；
- 资源相对有限，强调成本、体积、功耗和性能价格比；
- 程序通常固化在 Flash 等非易失存储器中；
- 需要专门的开发工具、宿主机、目标机和调试环境；
- 对实时性、安全性、可靠性和确定性要求较高。

“嵌入式系统一定追求通用性”通常是错误方向。它的优势恰恰是针对目标场景进行定制；题目出现功耗、体积、接口、实时响应、可靠性或环境温度时，优先从嵌入式约束判断。

### 1.3 实时系统与安全攸关系统

实时系统的正确性不仅取决于结果是否正确，还取决于<strong>是否在规定时间内产生结果</strong>。教材把嵌入式系统分为实时和非实时，实时系统还可进一步区分强实时和弱实时；按失效后果，还可分为安全攸关和非安全攸关系统。

| 概念 | 判断重点 |
| --- | --- |
| 实时 | 结果和时间约束同时正确 |
| 强实时 | 超过截止期限可能直接视为系统错误 |
| 安全攸关 | 功能错误或系统失效可能造成人员伤亡、财产损失等严重后果 |

### 1.4 嵌入式处理器与 MMU

| 类型 | 真题抓手 |
| --- | --- |
| MPU | 增强型通用微处理器，通常需要外围存储器和接口芯片 |
| MCU | 将 CPU、存储器、定时器和 I/O 等集成在单片芯片上，适合轻量控制 |
| DSP | 针对数字信号处理优化，常见特征是快速乘法、乘积累加和专用寻址 |
| SoC | 将处理器、存储器、I/O 和专用 IP 等集成为完整系统级芯片 |

MMU 题要抓住“处理器能力和操作系统需求的匹配”：题库中的 Cortex-M3 不具备 MMU，配套的是不依赖 MMU 的 UC/OS-II；需要虚拟内存或内存隔离能力的复杂操作系统，则通常要求处理器具备相应的内存管理能力。不要把 MPU 和 MMU 混为一谈：前者强调内存保护，后者承担更完整的地址映射和管理功能。

### 1.5 嵌入式软件架构

教材给出的典型五层结构是：

```text
应用层：工业控制、物联网、移动设备等具体应用
中间件层：数据库、图形、消息、通信等公共服务
操作系统层：任务、存储、设备、通信、时间和中断管理
抽象层：HAL、BSP，以及面向硬件的驱动支持
硬件层：处理器、存储器、I/O、总线、电源和时钟
```

其中，HAL 负责向上层抽象硬件资源；BSP 面向具体硬件芯片或电路提供驱动支持，帮助操作系统适配目标板卡。

真题还会把嵌入式系统的典型架构概括为<strong>层次化模式架构和递归模式架构</strong>。看到“层次化”时，联想到功能分层和清晰的上下层依赖；看到“递归”时，联想到组件或控制结构中的嵌套调用。

设计题中出现表驱动、动态处理、参数表或边界条件时，优先检查<strong>参数表一致性、边界检查、异常处理和可验证性</strong>。资源受限、实时性要求高的系统尤其需要避免越界、索引错误和不可预期状态。

<details>
<summary>相关真题：嵌入式 10 题</summary>

<QuestionBank :questions="embeddedQuestions" compact />

</details>

## 2. AI 芯片与专用处理器

这一部分在题库中既出现在“嵌入式”分类，也有一题单列为“AI 芯片”。复习时按“处理对象”和“可配置程度”区分，不要只背芯片名称。

### 2.1 AI 芯片的四个特征

真题给出的四个关键特征是：

1. 新型的计算范式；
2. 支持训练和推断；
3. 具备大数据处理能力；
4. 具备可重构能力。

### 2.2 GPU、NPU、FPGA、ASIC

| 类型 | 主要定位 | 题目中的关键词 |
| --- | --- | --- |
| GPU | 大规模并行计算，也用于图形处理和 AI 计算 | 并行、浮点运算、FLOPS |
| NPU | 神经网络计算专用处理器 | 计算速度快、性能好、功耗较低 |
| FPGA | 通过重新配置硬件逻辑适配任务 | 可编程、可重构、灵活 |
| ASIC | 针对特定任务定制的专用集成电路 | 专用、性能和能效突出 |

AI 芯片技术架构的常见答案是<strong>GPU、FPGA、ASIC</strong>。题目中的 FLOPS 表示每秒浮点运算次数，TFlops 表示万亿次浮点运算；遇到具体峰值数字时，按题干给出的时代背景和选项判断，不把数字当成永久不变的指标。

<details>
<summary>相关真题：AI 芯片 1 题</summary>

<QuestionBank :questions="chipQuestions" compact />

</details>

## 复习顺序

1. 先掌握嵌入式系统的特征、处理器分类、实时性、MMU 和软件架构。
2. 再用真题记忆单片机、SoC、GPU、NPU、FPGA、ASIC 的基本定位。
3. 对表驱动、动态处理、功耗、响应时间和可靠性等题干线索，优先从资源约束和实时性推理。

这部分的目标不是补齐完整的嵌入式专业课程，而是掌握真题中反复出现的系统特征、术语和判断线索。
