# AGENTS.md

系统架构设计师备考学习站（VitePress + GitHub Pages）。

## 规则

### 共享项目边界

- `docs/` 是 VitePress 发布目录，只有经过确认可以公开的页面、JSON 和静态资源才能进入这里。
- `local/` 是个人资料、本地数据库、处理脚本和本地 Skill 目录，不提交到仓库。
- `temp/` 是 PDF 转换、调试、渲染和验证的临时目录，不作为正式数据源。
- `share/` 只表示资料暂存位置，不代表资料自动拥有公开或再分发权限。
- 本机资料目录、生成物和本地处理细节见同目录下的 `AGENTS.local.md`；该文件被 Git 忽略，不属于仓库规则。

### 题库数据流

- PDF 只作为一次性录入来源，不作为网站构建时的数据源。
- 本地数据库和脚本负责结构化与导出，具体路径和命令见 `AGENTS.local.md`。
- JSON 是线上页面的数据源，统一放在 `docs/data/`；题库 JSON 固定放在 `docs/data/question-banks/`，按题库来源拆分文件。
- 页面通过导入 `docs/data/question-banks/*.json` 使用题库；VitePress 构建后会将这些 JSON 打包进静态页面，因此线上不需要后端服务。
- 题目中的图片作为线上资源放在 `docs/public/assets/` 下，并按大模块使用 `general/`、`case/`、`thesis/` 分类。
- 正常构建不应重新读取 PDF；发布前只使用已审核的 JSON 和静态资源。

### 学习资料来源与用途

- 真题、教材和本地笔记的具体路径见 `AGENTS.local.md`。
- 使用项目资料整理内容时，应区分真题原文、资料中的知识点和整理后的学习笔记，不得将模型自身知识替代项目资料作为答案来源。

### 变更约束

- 新增题库时，先明确 PDF 路径、科目名称、题目分类和考试批次，再执行导入和导出。
- 不把 `local/` 或 `temp/` 中的文件复制到线上目录；只有经过确认可以公开的 JSON、页面和图片才进入 `docs/`。
- 修改题库后需要检查 SQLite 完整性、JSON 校验结果和 `npm run docs:build` 构建结果。
- 写入 VitePress 页面 Markdown（`docs/`）时，需要强调的内容如果加粗标记后直接接中文，优先使用 `<strong>...</strong>`，避免使用容易解析不稳定的 `**...**中文`；这条只约束页面文件，不适用于聊天回复。
- AI 只基于项目中提供的笔记和资料工作，不把模型自身知识直接当作学习站答案来源。
